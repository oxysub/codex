# Manatal (or other FastAPI) base URL, no trailing slash. CV Formatter proxies to POST {URL}/cv/format.
# On Render, ensure OPENAI_API_KEY is set — parse_cv uses gpt-4o-mini before format_cv builds the .docx.
FASTAPI_BACKEND_URL=https://your-fastapi-service.onrender.com
